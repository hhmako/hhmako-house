interface:
  display_name: "Figma 设计方案拓展"
  short_description: "读取 Figma 与 PRD，复用原组件和 Token，生成并校验多状态页面。"
  default_prompt: "使用 $figma-design-expansion 读取现有 Figma 与 PRD，复用原组件和 Token 拓展完整状态，并在生成后读回校验。"

policy:
  allow_implicit_invocation: true
